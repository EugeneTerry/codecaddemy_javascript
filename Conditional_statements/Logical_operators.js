let mood = 'sleepy';
let tirednessLevel = 6;
if ( mood === 'sleepy' && tirednessLevel > 8) {
  console.log('time to sleep');
} else { console.log('not bed time yet')
}
/*
the and operator (&&)
the or operator (||)
the not operator, otherwise known as the bang operator (!)
*/
let fastFood = 'hungry'
let thirstLevel = 15
if( fastFood === 'hungry' && thirstLevel >=9) {
  console.log(`You need to order the whole combo`)
} else {
  console.log(`You can just order the sandwich only`)
}
