// check the data type of a variable’s value, you can use the typeof operator
let newVariable = 'Playing around with typeof.';
console.log(typeof newVariable);
// this diplays the word "string" because the variable has letters in it
newVariable = 1 //changed the string variable to a number
console.log(typeof newVariable);
// this diplays the word "number" because the variable is a number now
